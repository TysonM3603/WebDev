//let - default

let myName = "0";
let number = 25;
let isHungry = true;

let result = myName + number;

//const - constant. Doesn't prevent you from changing it.

const pieVal = 3.14;

//var - don't use in js

//js says that this is undefined. Declared but not assigned.
let thisVar;

//null is intentional.
let someOtherThing = null;

console.log(myName);
console.log(someOtherThing);

//object
let person = {
   name: "John",
   age: 25
};

//array
let colors = ["red", "blue", "green"];

console.log(colors[0]);